import 'package:fluent_ui/fluent_ui.dart';

class KeystrokeButton extends StatefulWidget {
  const KeystrokeButton({super.key});

  @override
  State<KeystrokeButton> createState() => _KeystrokeButtonState();
}

class _KeystrokeButtonState extends State<KeystrokeButton> {
  @override
  Widget build(BuildContext context) {
    return const Text("Keystroke Button");
  }
}
